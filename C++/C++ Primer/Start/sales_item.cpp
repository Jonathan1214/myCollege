#include <iostream>
#include "Sales_item.h"
/*
* Sales_item 类型对象支持操作
* 1. 调用一个名为 isbn 的函数从一个 Sales_item 对象中读取 ISBN
* 2. 用输入 >> 和输出运算符 << 读写 Sales_item 类型对象
* 3. = 赋值
* 4. + 把两个 Sales_item 类型对象相加，对象要表示同一本书ISBN相同 总销售额相加
* 5. 支持 +=
*/
int main()
{
    Sales_item book;
    return 0;
}
