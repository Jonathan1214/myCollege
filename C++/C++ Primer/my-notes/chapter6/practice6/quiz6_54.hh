#ifndef QUIZ6_54_HH
#define QUIZ6_54_HH
#include <vector>

// 测试函数
int func(int, int);

// vector

// using funcV = std::vector<decltype(func) *>;

// typedef std::vector<decltype(func) *> funcV;
// 不能这么定义么

#endif