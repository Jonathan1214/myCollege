#ifndef ELIMDUPS_HH
#define ELIMDUPS_HH
#include <algorithm>
#include <vector>
#include <string>
void elimDups(std::vector<std::string> &words);

bool isShorter(const std::string&, const std::string&);

#endif